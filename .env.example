# Run `pnpm run setup:all` to create service `.env` files from local examples.
# Set orchestrator API key before using real providers.

# apps/orchestrator/.env
OPENROUTER_API_KEY=
USE_REAL_PROVIDERS=true
IMAGE_PROVIDER_URL=http://localhost:8020
TTS_URL=http://localhost:8010
TRANSCRIBER_URL=http://localhost:8030

# apps/tts/.env
DEVICE=mps
OUTPUT_DIR=outputs

# apps/image-provider/.env
PORT=8020
BROWSER_HEADLESS=true

# apps/transcriber/.env
WHISPERX_MODEL=large-v3
WHISPERX_DEVICE=auto
WHISPERX_COMPUTE_TYPE=int8
WHISPERX_LANGUAGE=
WHISPERX_FORCE_TRANSCRIBE=0
